# CLEANUP_PLAN.md

Before final merge:

- consolidate overlapping closeout notes into canonical docs;
- remove/archive temporary `WORK_NOTES`, `EXECUTION_LOG`, status scratch files if redundant;
- keep ADR/specs that add durable value;
- ensure documentation index references only intended long-lived files.
