# OPS_DATA_SOURCES.md

Operational source rules:

- live structured state → Supabase;
- Sheet → mirror/export;
- docs → durable product/ops rules;
- Vercel → deployment/runtime;
- GitHub → code/migrations/history;
- offline pack → contingency snapshot, not a new source of truth after reconnect.
